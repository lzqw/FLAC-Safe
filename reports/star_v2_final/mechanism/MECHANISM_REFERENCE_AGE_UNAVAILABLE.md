# Mechanism Reference-Age Dynamics Unavailable

Final resume_300k mechanism logs contain paired corridor/current audit diagnostics, but `reference_age_post_update` is collapsed to a single value. The Figure 2 mechanism plot therefore uses logged paired-audit batches as mechanism validation evidence, while age-dynamic claims are not made.

- observed_reference_age_values: `[1]`
- mechanism_rows_used: `1051`
